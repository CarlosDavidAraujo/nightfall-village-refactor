/* import { Player } from "../player/Player"

const actions = {
  vote: (author: Player, target: Player) => {
    target.increaseVotesCount()
  },
  "show-role": (author: Player, target: Player) => {
    return `${target.getName()} é um(a) ${target.getRole().getName()}`
  },
  'mark-to-death': (author: Player, target: Player) => {
  target.
  }
}
 */
